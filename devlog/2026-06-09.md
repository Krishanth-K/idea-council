The second day of coding this project. Modified the API call of arxiv to returns the summary instead of blob

Then wrote down the architecture on a paper, to make it solid

Modifed the debate idea a bit:
- There are multiple lawyers, advocating for a criteria
- There is a judge, who gives the final results

Two rounds:
- First round: All lawyers present their statements
- Second round: Lawyers could cross examine other laywers based on their statements

